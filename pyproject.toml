[project]
name = "joke-cli"
version = "0.1.0"
description = "Practicing uv package management with a joke cli"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "requests>=2.32.5",
]


[tool.uv]
package= true

[project.scripts]
joke = "main:get_joke"